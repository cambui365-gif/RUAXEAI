name: ruaxeai_tablet
description: RUAXEAI - Tablet Kiosk App cho trạm rửa xe tự phục vụ
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  qr_flutter: ^4.1.0
  usb_serial: ^0.5.1
  wakelock_plus: ^1.1.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
